apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
images:
- name: controller
  newName: docker.io/nextlinux/engine-operator
  newTag: v1.0.1

resources:
- manager.yaml

generatorOptions:
  disableNameSuffixHash: true
