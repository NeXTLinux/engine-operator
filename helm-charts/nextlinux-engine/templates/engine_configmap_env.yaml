kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ template "nextlinux-engine.fullname" . }}-env
  labels:
    app: {{ template "nextlinux-engine.fullname" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- with .Values.nextlinuxGlobal.labels }}
    {{ toYaml . | nindent 4 }}
    {{- end }}
data:
  NEXTLINUX_DB_NAME: {{ index .Values "postgresql" "postgresDatabase" | quote }}
  NEXTLINUX_DB_USER: {{ index .Values "postgresql" "postgresUser" | quote }}
  {{- if and (index .Values "postgresql" "externalEndpoint") (not (index .Values "postgresql" "enabled")) }}
  NEXTLINUX_DB_HOST: {{ index .Values "postgresql" "externalEndpoint" | quote }}
  {{- else if and (index .Values "cloudsql" "enabled") (not (index .Values "postgresql" "enabled")) }}
  NEXTLINUX_DB_HOST: "localhost:5432"
  {{- else }}
  NEXTLINUX_DB_HOST: "{{ template "postgres.fullname" . }}:5432"
  {{- end }}
